require 'spec_helper'

describe React::Tags::Rails do
  it 'has a version number' do
    expect(React::Tags::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
